void InsertionSort(Nodo *lista){
	Nodo *viaje = lista, *pos;
	int i, aux;

	Color RGB;
    /*
	while (viaje != NULL){
		pos = viaje;
		RGB = viaje->RGB;

		while ((pos>0)&&(numeros[pos-1]> aux)){
			numeros[pos]= numeros[pos -1];
			pos--;
		}

		viaje->RGB = aux;
	}

	return 0;
	*/
}
